{
  "name": "openclaw-claude-bridge",
  "version": "1.0.0",
  "description": "OpenAI-compatible proxy bridging OpenClaw to Claude Code CLI",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "postinstall": "cd dashboard && npm install --ignore-scripts && npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
